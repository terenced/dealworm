export interface Book {
  title: string;
  imageUrl: string;
  description: string;
  isbn: string;
  published: string;
  url: string;
  price?: number;
  created?: number;
  updated?: number;
  // storeUrl?: string;
}
